-- 12: Unzip --
-- Size Challenge: 11/8 --
-- Speed Challenge: 18s/7s --
-- Move the leftmost data cube one tile up. --
-- Move the next data cube one tile down. --
-- Next up. Next down. --
-- And so on, like unzipping a zipper. --

pickup c
if w == wall:
  step n
  drop
endif

loop:
if nw == datacube:
  step s
  drop
endif

if sw == datacube:
  step n
  drop
endif
jump loop