-- 12: Unzip --
-- Size Challenge: 8/8 --
-- Move the leftmost data cube one tile up. --
-- Move the next data cube one tile down. --
-- Next up. Next down. --
-- And so on, like unzipping a zipper. --

pickup c

loop:
if nw == datacube:
  step s
  drop
endif

if sw == datacube or w == wall:
  step n
  drop
endif
jump loop